% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workout_comparison.R
\name{plot_workout_sprints}
\alias{plot_workout_sprints}
\title{Plot a multi-sprint workout comparison}
\usage{
plot_workout_sprints(
  motion_data,
  sprints = NULL,
  signal = c("speed", "external_power", "metabolic_power"),
  x = c("distance", "time"),
  sprint_ids = NULL,
  cost_running_flat = 3.6,
  slope_equation = "extended",
  ...
)
}
\arguments{
\item{motion_data}{A workout data frame with \code{time} (s) and \code{velocity} (m/s).}

\item{sprints}{Optional sprint table from \code{\link[=detect_sprints]{detect_sprints()}}; if \code{NULL} (default),
sprints are detected with \code{\link[=detect_sprints]{detect_sprints()}}.}

\item{signal}{Signal to plot: \code{"speed"} (default), \code{"external_power"} or
\code{"metabolic_power"}.}

\item{x}{Horizontal axis: \code{"distance"} (m, default) or \code{"time"} (s), measured
from each sprint's start.}

\item{sprint_ids}{Optional vector of \code{sprint_id}s to display; if \code{NULL} (default),
all detected sprints are shown.}

\item{cost_running_flat}{Flat-terrain cost of running (J/kg/m) for metabolic power.}

\item{slope_equation}{Slope equation passed to \code{\link[=cost_running]{cost_running()}}.}

\item{...}{Passed to \code{\link[=detect_sprints]{detect_sprints()}} when \code{sprints} is \code{NULL}.}
}
\value{
A ggplot object.
}
\description{
Overlays each detected sprint's signal (speed, external or metabolic power)
against distance or time from the sprint start, one coloured line per sprint,
in the shared package style. The default (speed vs distance) reproduces the
classic sprint-comparison figure.
}
\examples{
gpexe <- subset(ten_200_sprints_paired, source == "gpexe")
plot_workout_sprints(gpexe, sprint_ids = c(1, 5, 9))
}
