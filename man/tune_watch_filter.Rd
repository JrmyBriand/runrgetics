% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/watch_filter.R
\name{tune_watch_filter}
\alias{tune_watch_filter}
\title{Tune the watch filter against the gpexe reference}
\usage{
tune_watch_filter(
  paired_data,
  methods = c("butterworth", "savitzky_golay", "moving_average"),
  speed_sources = c("reported", "gps"),
  cutoffs = seq(0.1, 0.45, by = 0.05),
  windows = c(5, 7, 9, 11, 15),
  orders = 2:3,
  target_hz = 5,
  reference = "gpexe",
  device = "polar_stryd"
)
}
\arguments{
\item{paired_data}{a paired-device data frame with a \code{source} column (e.g.
\link{sprint_mix_paired}), containing both the reference and watch rows.}

\item{methods}{filter families to evaluate.}

\item{speed_sources}{watch speed inputs to evaluate (\code{"reported"}, \code{"gps"}).}

\item{cutoffs}{Butterworth cut-off frequencies to sweep (Hz).}

\item{windows}{window lengths to sweep (Savitzky-Golay / moving average).}

\item{orders}{filter / polynomial orders to sweep.}

\item{target_hz}{comparison grid rate (Hz).}

\item{reference, device}{values of the \code{source} column identifying the reference
(gpexe) and the watch rows.}
}
\value{
a list with \code{results} (a \link[tibble:tibble]{tibble} of every configuration
scored), \code{baseline} (unfiltered watch metrics per speed source) and \code{best}
(the single best-scoring row).
}
\description{
Sweeps filter families and parameters, applies each to the watch data with
\code{\link[=filter_watch_motion]{filter_watch_motion()}}, and scores the result against the time-aligned gpexe
reference with \code{\link[=motion_agreement]{motion_agreement()}}. Returns the full scored grid plus the best
configuration (lowest combined speed + acceleration RMSE), and the unfiltered
baselines for comparison. Used to derive the package defaults and to re-tune on
new paired sessions.
}
\examples{
\donttest{
tuned <- tune_watch_filter(sprint_mix_paired, methods = "butterworth",
                           speed_sources = "reported", cutoffs = c(0.2, 0.3))
tuned$best
}
}
