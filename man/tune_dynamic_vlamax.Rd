% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dynamic_bioenergetics.R
\name{tune_dynamic_vlamax}
\alias{tune_dynamic_vlamax}
\title{Calibrate the dynamic model's VLamax to a metabolic-power demand}
\usage{
tune_dynamic_vlamax(
  power_demand,
  time,
  vo2max,
  sc = 25,
  sa = 7,
  m_active = 0.25,
  pcr_0 = 23,
  initial_bla = 1.1,
  vlamax_range = c(0.1, 1.5)
)
}
\arguments{
\item{power_demand}{Numeric vector of metabolic-power demand (W/kg), the series
the model reproduces (the same di Prampero demand the sprint bioenergetic
model fits).}

\item{time}{Numeric vector of time points (s), strictly increasing, the same
length as \code{power_demand}.}

\item{vo2max}{Maximal oxygen uptake (mL/kg/min, systemic). For the dynamic
model use the measured value over-estimated by ~5\%.}

\item{sc}{Creatine pool size (mmol/kg). Default 25 (MuscleEdot default).}

\item{sa}{Adenylate pool size (mmol/kg). Default 7 (MuscleEdot default).}

\item{m_active}{Active muscle mass fraction. Default 0.25 (MuscleEdot default).}

\item{pcr_0}{Initial phosphocreatine concentration (mmol/kg). Default 23.}

\item{initial_bla}{Initial blood lactate (mmol/L). Default 1.1.}

\item{vlamax_range}{Numeric length-2 search interval for VLamax
(mmol/kg/s, systemic). Default \code{c(0.1, 1.5)}.}
}
\value{
A list with \code{vlamax} (the tuned value, mmol/kg/s) and \code{rmse} (the
minimised RMSE between total modelled power and \code{power_demand}, W/kg).
}
\description{
Fixes VO2max (and the other parameters) and finds the VLamax that minimises the
RMSE between the dynamic model's total metabolic power and the observed
di Prampero power demand over the effort, by 1-D optimisation
(\code{\link[stats:optimize]{stats::optimize()}}).
}
\examples{
t <- seq(0, 20, by = 0.5)
demand <- 25 * (1 - exp(-t / 3)) + 8 * exp(-t / 6)
tune_dynamic_vlamax(demand, t, vo2max = 78.75, vlamax_range = c(0.2, 1.0))
}
