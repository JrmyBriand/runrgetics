% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_running_power.R
\name{compute_activity_metabolic_running_power}
\alias{compute_activity_metabolic_running_power}
\title{Compute metabolic running power from an activity file}
\usage{
compute_activity_metabolic_running_power(
  file_path,
  file_type = "fit",
  cost_running_flat = 3.6,
  slope_equation = "extended",
  smoothing = "none",
  window = 3,
  order = 2,
  cfreq = 0.1
)
}
\arguments{
\item{file_path}{Path to the activity file.}

\item{file_type}{Either \code{"fit"} (default) or \code{"csv"}.}

\item{cost_running_flat}{Flat-terrain cost of running (J/kg/m).}

\item{slope_equation}{Slope equation passed to \code{\link[=cost_running]{cost_running()}}.}

\item{smoothing}{Velocity smoothing method passed to \code{\link[=smooth_velocity]{smooth_velocity()}}.}

\item{window}{Smoothing window length (samples).}

\item{order}{Smoothing / Butterworth order.}

\item{cfreq}{Butterworth normalised cut-off.}
}
\value{
A data frame with metabolic power columns (see \code{\link[=compute_metabolic_running_power]{compute_metabolic_running_power()}}).
}
\description{
Reads an activity file (\code{.fit} or \code{.csv}) and computes metabolic running power
with \code{\link[=compute_metabolic_running_power]{compute_metabolic_running_power()}}.
}
\examples{
\donttest{
f <- system.file("extdata/activities/sample_run.fit", package = "runrgetics")
if (nzchar(f)) compute_activity_metabolic_running_power(f)
}
}
