% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sprint_energy.R
\name{plot_sprint_energy}
\alias{plot_sprint_energy}
\title{Plot cumulative energy by pathway for one sprint}
\usage{
plot_sprint_energy(
  motion_data,
  sprint_id = 1,
  sprints = NULL,
  maximal_aerobic_power = 27,
  x = c("distance", "time"),
  ...
)
}
\arguments{
\item{motion_data}{A workout data frame with \code{time} (s) and \code{velocity} (m/s).}

\item{sprint_id}{Which detected sprint to plot (its \code{sprint_id}).}

\item{sprints}{Optional sprint table from \code{\link[=detect_sprints]{detect_sprints()}}; if \code{NULL} (default),
sprints are detected with \code{\link[=detect_sprints]{detect_sprints()}}.}

\item{maximal_aerobic_power}{Maximal aerobic power, MAP (W/kg). Use 27 for the
\code{sprint_mix} / \code{ten_200_sprints} sessions.}

\item{x}{Horizontal axis: \code{"distance"} (m, default) or \code{"time"} (s).}

\item{...}{Additional arguments passed to \code{\link[=sprint_energy_data]{sprint_energy_data()}}.}
}
\value{
A ggplot object.
}
\description{
Shows how metabolic energy accumulates as the sprint progresses: the alactic,
lactic and aerobic cumulative energies are drawn as stacked filled areas, so the
top of the stack at any point is the cumulative total energy (J/kg). For example,
if 350 J/kg are expended over the sprint and half by 50 m, the stack reaches 175
at 50 m.
}
\examples{
gpexe <- subset(ten_200_sprints_paired, source == "gpexe")
plot_sprint_energy(gpexe, sprint_id = 1, maximal_aerobic_power = 27)
}
