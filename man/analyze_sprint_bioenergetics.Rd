% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/training_bioenergetics.R
\name{analyze_sprint_bioenergetics}
\alias{analyze_sprint_bioenergetics}
\title{Bioenergetic analysis of a single sprint}
\usage{
analyze_sprint_bioenergetics(
  sprint_df,
  maximal_aerobic_power = 27,
  trim = TRUE,
  decel_threshold = 1,
  launch_accel = 0.5,
  cost_running_flat = 3.6,
  slope_equation = "extended",
  mu = -0.4,
  sigma = 1,
  k1 = 2.75,
  k2 = 35,
  fit_mu = TRUE
)
}
\arguments{
\item{sprint_df}{A single sprint's data frame with \code{time} (s) and \code{velocity}
(m/s); an \code{acceleration} column is used if present, otherwise acceleration is
derived from velocity. Metabolic power is computed with \code{\link[=cost_running_sprint]{cost_running_sprint()}}.}

\item{maximal_aerobic_power}{Maximal aerobic power, MAP (W/kg). Use 27 for the
\code{sprint_mix} / \code{ten_200_sprints} sessions.}

\item{trim}{If \code{TRUE} (default), trim the pre-launch lead-in and the
end-of-effort deceleration before fitting.}

\item{decel_threshold}{Deceleration threshold (m/s^2) for the end-of-effort trim.}

\item{launch_accel}{Acceleration threshold (m/s^2) for the launch trim.}

\item{cost_running_flat}{Flat-terrain cost of running (J/kg/m).}

\item{slope_equation}{Slope equation passed to \code{\link[=cost_running]{cost_running()}}.}

\item{mu, sigma, k1, k2}{Sprint bioenergetic model shape parameters
(see \code{\link[=sprint_bioenergetic_model]{sprint_bioenergetic_model()}}). \code{mu} is the starting value when \code{fit_mu}
is \code{TRUE}, otherwise the fixed value.}

\item{fit_mu}{If \code{TRUE} (default), also estimate the alactic peak location \code{mu}
so it adapts to the observed metabolic-power peak (recommended for gpexe /
training sprints, whose power peaks later than in maximal sprints); if \code{FALSE},
\code{mu} is held at the published fixed value.}
}
\value{
A one-row \link[tibble:tibble]{tibble} with \code{duration}, \code{maximal_alactic_power},
\code{maximal_lactic_power}, \code{mu} (the alactic peak location used), peak and mean
total power, the alactic / lactic / aerobic energies (J/kg) and their
percentage contributions, or a row of \code{NA}s if the model fit fails.
}
\description{
Trims the pre-launch lead-in (\code{\link[=trim_sprint_launch]{trim_sprint_launch()}}) and the end-of-effort
deceleration (\code{\link[=trim_sprint_deceleration]{trim_sprint_deceleration()}}), computes the sprint metabolic power
(sprint cost of running, \code{\link[=cost_running_sprint]{cost_running_sprint()}}, on smoothed acceleration), fits
the sprint bioenergetic model (\code{\link[=sprint_bioenergetic_model_fit]{sprint_bioenergetic_model_fit()}}) with the
supplied maximal aerobic power, and returns the alactic / lactic / aerobic peak
powers, energies and percentage contributions.
}
\examples{
gpexe <- subset(ten_200_sprints_paired, source == "gpexe")
sprints <- detect_sprints(gpexe)
one <- gpexe[sprints$start_index[1]:sprints$end_index[1], ]
analyze_sprint_bioenergetics(one, maximal_aerobic_power = 27)
}
