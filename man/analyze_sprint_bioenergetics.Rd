% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/training_bioenergetics.R
\name{analyze_sprint_bioenergetics}
\alias{analyze_sprint_bioenergetics}
\title{Bioenergetic analysis of a single sprint}
\usage{
analyze_sprint_bioenergetics(
  sprint_df,
  maximal_aerobic_power = 27,
  trim = TRUE,
  decel_threshold = 1,
  cost_running_flat = 3.6,
  slope_equation = "extended",
  mu = -0.4,
  sigma = 1,
  k1 = 2.75,
  k2 = 35
)
}
\arguments{
\item{sprint_df}{A single sprint's data frame with \code{time} (s) and \code{velocity}
(m/s); a \code{metabolic_power} column is used if present, otherwise it is computed
with \code{\link[=compute_metabolic_running_power]{compute_metabolic_running_power()}}.}

\item{maximal_aerobic_power}{Maximal aerobic power, MAP (W/kg). Use 27 for the
\code{sprint_mix} / \code{ten_200_sprints} sessions.}

\item{trim}{If \code{TRUE} (default), trim the end-of-effort deceleration before fitting.}

\item{decel_threshold}{Deceleration threshold (m/s^2) for the trim.}

\item{cost_running_flat}{Flat-terrain cost of running (J/kg/m).}

\item{slope_equation}{Slope equation passed to \code{\link[=cost_running]{cost_running()}}.}

\item{mu, sigma, k1, k2}{Sprint bioenergetic model shape parameters
(see \code{\link[=sprint_bioenergetic_model]{sprint_bioenergetic_model()}}).}
}
\value{
A one-row \link[tibble:tibble]{tibble} with \code{duration}, \code{maximal_alactic_power},
\code{maximal_lactic_power}, peak and mean total power, the alactic / lactic / aerobic
energies (J/kg) and their percentage contributions, or a row of \code{NA}s if the
model fit fails.
}
\description{
Computes metabolic power over a sprint (if not already present), trims the
end-of-effort deceleration (\code{\link[=trim_sprint_deceleration]{trim_sprint_deceleration()}}), fits the sprint
bioenergetic model (\code{\link[=sprint_bioenergetic_model_fit]{sprint_bioenergetic_model_fit()}}) with the supplied
maximal aerobic power, and returns the alactic / lactic / aerobic peak powers,
energies and percentage contributions.
}
\examples{
gpexe <- subset(ten_200_sprints_paired, source == "gpexe")
sprints <- detect_sprints(gpexe)
one <- gpexe[sprints$start_index[1]:sprints$end_index[1], ]
analyze_sprint_bioenergetics(one, maximal_aerobic_power = 27)
}
