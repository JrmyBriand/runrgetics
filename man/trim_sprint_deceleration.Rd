% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/training_bioenergetics.R
\name{trim_sprint_deceleration}
\alias{trim_sprint_deceleration}
\title{Trim a sprint's end-of-effort deceleration}
\usage{
trim_sprint_deceleration(sprint_df, decel_threshold = 1)
}
\arguments{
\item{sprint_df}{A single sprint's data frame with \code{time} (s) and \code{velocity}
(m/s) columns (an \code{acceleration} column is used if present).}

\item{decel_threshold}{Deceleration magnitude (m/s^2) marking the abrupt finish.}
}
\value{
The input data frame truncated at the sprint finish.
}
\description{
Finds the "sprint finish" - the onset of the abrupt, end-of-effort braking -
and drops everything after it. After the peak (smoothed) velocity, the finish
is the first sample whose (smoothed) acceleration falls below \code{-decel_threshold}.
The gradual, natural in-sprint deceleration (small negative acceleration) is
retained; only the sharp end braking is removed. If no abrupt deceleration is
found the sprint is returned unchanged.
}
\examples{
gpexe <- subset(ten_200_sprints_paired, source == "gpexe")
sprints <- detect_sprints(gpexe)
one <- gpexe[sprints$start_index[1]:sprints$end_index[1], ]
nrow(trim_sprint_deceleration(one))
}
