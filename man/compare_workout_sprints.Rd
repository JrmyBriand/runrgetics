% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workout_comparison.R
\name{compare_workout_sprints}
\alias{compare_workout_sprints}
\title{Compare sprints within a workout}
\usage{
compare_workout_sprints(
  motion_data,
  sprints = NULL,
  cost_running_flat = 3.6,
  slope_equation = "extended",
  ...
)
}
\arguments{
\item{motion_data}{A workout data frame with \code{time} (s) and \code{velocity} (m/s).}

\item{sprints}{Optional sprint table from \code{\link[=detect_sprints]{detect_sprints()}}; if \code{NULL} (default),
sprints are detected with \code{\link[=detect_sprints]{detect_sprints()}}.}

\item{cost_running_flat}{Flat-terrain cost of running (J/kg/m) for metabolic power.}

\item{slope_equation}{Slope equation passed to \code{\link[=cost_running]{cost_running()}}.}

\item{...}{Passed to \code{\link[=detect_sprints]{detect_sprints()}} when \code{sprints} is \code{NULL}.}
}
\value{
A \link[tibble:tibble]{tibble} with one row per sprint: \code{sprint_id},
\code{start_time}, \code{duration}, \code{distance}, \code{peak_speed}, \code{mean_speed},
\code{peak_external_power}, \code{mean_external_power}, \code{peak_metabolic_power},
\code{mean_metabolic_power}.
}
\description{
Detects sprints (or uses supplied ones) in a workout and summarises and compares
them: per-sprint peak/mean speed, distance, duration and peak/mean external and
metabolic power. Intended to be run on gpexe data.
}
\examples{
gpexe <- subset(ten_200_sprints_paired, source == "gpexe")
compare_workout_sprints(gpexe)
}
