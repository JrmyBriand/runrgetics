% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/external_power.R
\name{compute_external_running_power}
\alias{compute_external_running_power}
\title{Compute external running power over a run}
\usage{
compute_external_running_power(
  running_data_frame,
  smoothing = "none",
  window = 3,
  order = 2,
  cfreq = 0.1
)
}
\arguments{
\item{running_data_frame}{A data frame with \code{time} (s) and \code{velocity} (m/s) columns.}

\item{smoothing}{Velocity smoothing method passed to \code{\link[=smooth_velocity]{smooth_velocity()}}.}

\item{window}{Smoothing window length (samples).}

\item{order}{Smoothing / Butterworth order.}

\item{cfreq}{Butterworth normalised cut-off.}
}
\value{
The input data frame with \code{acceleration} (m/s^2) and \code{external_power}
(W/kg) columns added.
}
\description{
Smooths velocity (optionally), computes acceleration and the instantaneous
external mechanical power (\code{\link[=external_power]{external_power()}}) for a running data frame.
}
\examples{
run <- tibble::tibble(time = 0:9,
                      velocity = c(0, 2, 4, 6, 7, 7.5, 7.6, 7.6, 7.5, 7.4))
compute_external_running_power(run)
}
