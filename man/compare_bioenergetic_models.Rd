% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dynamic_vs_sprint.R
\name{compare_bioenergetic_models}
\alias{compare_bioenergetic_models}
\title{Compare the dynamic and sprint bioenergetic partitions of a sprint}
\usage{
compare_bioenergetic_models(
  motion_data,
  sprint_id = 1,
  sprints = NULL,
  maximal_aerobic_power = 27,
  vo2max = 78.75,
  vlamax = NULL,
  vlamax_range = c(0.1, 1.5),
  sc = 25,
  sa = 7,
  m_active = 0.25,
  pcr_0 = 23,
  initial_bla = 1.1,
  ...
)
}
\arguments{
\item{motion_data}{A workout data frame with \code{time} (s) and \code{velocity} (m/s).}

\item{sprint_id}{Which detected sprint to compare (its \code{sprint_id}).}

\item{sprints}{Optional sprint table from \code{\link[=detect_sprints]{detect_sprints()}}; if \code{NULL}
(default), sprints are detected with \code{\link[=detect_sprints]{detect_sprints()}}.}

\item{maximal_aerobic_power}{Maximal aerobic power, MAP (W/kg), for the sprint
bioenergetic model. Default 27.}

\item{vo2max}{Dynamic-model VO2max (mL/kg/min, systemic; use the measured value
over-estimated by ~5\%). Default 78.75.}

\item{vlamax}{Dynamic-model VLamax (mmol/kg/s, systemic). If \code{NULL} (default)
it is tuned to the demand over \code{vlamax_range}.}

\item{vlamax_range}{Search interval for the VLamax tuning. Default \code{c(0.1, 1.5)}.}

\item{sc, sa, m_active, pcr_0, initial_bla}{Dynamic-model parameters, passed to
\code{\link[=simulate_dynamic_bioenergetics]{simulate_dynamic_bioenergetics()}}.}

\item{...}{Sprint bioenergetic model arguments passed through to
\code{\link[=analyze_sprint_bioenergetics]{analyze_sprint_bioenergetics()}} (e.g. \code{trim}, \code{cost_running_flat}, \code{mu}).}
}
\value{
A tidy \link[tibble:tibble]{tibble} with columns \code{model}
(\code{"dynamic"} / \code{"sprint"}), \code{pathway} (\code{"alactic"} / \code{"lactic"} /
\code{"aerobic"}), \code{energy_j_kg} (J/kg) and \code{pct} (\% of that model's total).
The tuned \code{vlamax} and the dynamic-vs-demand \code{rmse} are attached as attributes.
}
\description{
Runs both bioenergetic models on the same sprint, driven by the identical
di Prampero metabolic-power demand: the sprint bioenergetic model
(\code{\link[=analyze_sprint_bioenergetics]{analyze_sprint_bioenergetics()}}) and the dynamic Mader-style ODE model
(\code{\link[=simulate_dynamic_bioenergetics]{simulate_dynamic_bioenergetics()}}). VO2max is fixed; the dynamic model's
VLamax is tuned to the demand (\code{\link[=tune_dynamic_vlamax]{tune_dynamic_vlamax()}}) unless supplied.
Returns a tidy table of each pathway's energy (J/kg) and percentage share for
both models.
}
\examples{
gpexe <- subset(ten_200_sprints_paired, source == "gpexe")
# supply VLamax to skip tuning (fast); omit `vlamax` to tune it to the demand
cmp <- compare_bioenergetic_models(gpexe, sprint_id = 1,
                                   maximal_aerobic_power = 27,
                                   vo2max = 78.75, vlamax = 0.5)
cmp
\donttest{
# tuning VLamax to the di Prampero demand (slower):
cmp_tuned <- compare_bioenergetic_models(gpexe, sprint_id = 1, vo2max = 78.75)
attr(cmp_tuned, "vlamax")
}
}
