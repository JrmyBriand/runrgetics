% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/training_bioenergetics.R
\name{trim_sprint_launch}
\alias{trim_sprint_launch}
\title{Trim a sprint's pre-launch lead-in}
\usage{
trim_sprint_launch(sprint_df, launch_accel = 0.5)
}
\arguments{
\item{sprint_df}{A single sprint's data frame with \code{time} (s) and \code{velocity}
(m/s) columns (an \code{acceleration} column is used if present).}

\item{launch_accel}{Acceleration (m/s^2) marking the launch onset.}
}
\value{
The input data frame truncated at its start to begin at the launch.
}
\description{
Drops the low-speed lead-in before the acceleration burst so that the sprint
starts at the launch. The launch is the first sample (up to the peak velocity)
whose smoothed acceleration exceeds \code{launch_accel}; one sample before it is kept
so the very start of the burst is included.
}
\examples{
gpexe <- subset(ten_200_sprints_paired, source == "gpexe")
sprints <- detect_sprints(gpexe)
one <- gpexe[sprints$start_index[1]:sprints$end_index[1], ]
nrow(trim_sprint_launch(one))
}
