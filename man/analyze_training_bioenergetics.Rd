% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/training_bioenergetics.R
\name{analyze_training_bioenergetics}
\alias{analyze_training_bioenergetics}
\title{Bioenergetic analysis of a sprint-training workout}
\usage{
analyze_training_bioenergetics(
  motion_data,
  sprints = NULL,
  maximal_aerobic_power = 27,
  trim = TRUE,
  decel_threshold = 1,
  launch_accel = 0.5,
  cost_running_flat = 3.6,
  slope_equation = "extended",
  mu = 0.5,
  sigma = 0.5,
  k1 = 2.75,
  k2 = 35,
  fit_mu = TRUE,
  fit_sigma = TRUE,
  fit_k2 = TRUE,
  ...
)
}
\arguments{
\item{motion_data}{A workout data frame with \code{time} (s) and \code{velocity} (m/s).}

\item{sprints}{Optional sprint table from \code{\link[=detect_sprints]{detect_sprints()}}; if \code{NULL} (default),
sprints are detected with \code{\link[=detect_sprints]{detect_sprints()}}.}

\item{maximal_aerobic_power}{Maximal aerobic power, MAP (W/kg). Use 27 for the
\code{sprint_mix} / \code{ten_200_sprints} sessions.}

\item{trim}{If \code{TRUE} (default), trim the pre-launch lead-in and the
end-of-effort deceleration before fitting.}

\item{decel_threshold}{Deceleration threshold (m/s^2) for the end-of-effort trim.}

\item{launch_accel}{Acceleration threshold (m/s^2) for the launch trim.}

\item{cost_running_flat}{Flat-terrain cost of running (J/kg/m).}

\item{slope_equation}{Slope equation passed to \code{\link[=cost_running]{cost_running()}}.}

\item{mu, sigma, k1, k2}{Sprint bioenergetic model shape parameters
(see \code{\link[=sprint_bioenergetic_model]{sprint_bioenergetic_model()}}). The defaults (\code{mu = 0.5}, \code{sigma = 0.5},
\code{k2 = 35}) are starting values tuned for gpexe / training sprints; each is the
fitted starting value when its \verb{fit_*} switch is \code{TRUE}, otherwise the fixed value.}

\item{fit_mu}{If \code{TRUE} (default), also estimate the alactic peak location \code{mu}
so it adapts to the observed metabolic-power peak (training sprints peak later
than maximal sprints); if \code{FALSE}, \code{mu} is held fixed.}

\item{fit_sigma}{If \code{TRUE} (default), also estimate the alactic width \code{sigma}
(letting the bump narrow / decay faster); if \code{FALSE}, \code{sigma} is held fixed.}

\item{fit_k2}{If \code{TRUE} (default), also estimate the lactic decay time constant
\code{k2}. Fitting \code{mu}, \code{sigma} and \code{k2} together (with the tuned starts) gives
stable, physiological partitions across training sprints; if \code{FALSE}, \code{k2} is
held fixed.}

\item{...}{Passed to \code{\link[=detect_sprints]{detect_sprints()}} when \code{sprints} is \code{NULL}.}
}
\value{
A list with \code{per_sprint} (a \link[tibble:tibble]{tibble} of
\code{\link[=analyze_sprint_bioenergetics]{analyze_sprint_bioenergetics()}} rows, one per sprint, with a leading
\code{sprint_id}) and \code{summary} (a one-row tibble of workout means: mean energies
and mean percentage contributions across sprints).
}
\description{
Detects the sprints in a workout (or uses supplied ones), runs the bioenergetic
decomposition on each (\code{\link[=analyze_sprint_bioenergetics]{analyze_sprint_bioenergetics()}}) with the given maximal
aerobic power, and returns the per-sprint results plus a workout-level summary.
}
\examples{
gpexe <- subset(ten_200_sprints_paired, source == "gpexe")
res <- analyze_training_bioenergetics(gpexe, maximal_aerobic_power = 27)
res$summary
}
