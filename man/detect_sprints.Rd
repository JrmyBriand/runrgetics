% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sprint_detection.R
\name{detect_sprints}
\alias{detect_sprints}
\title{Detect sprints / high-intensity efforts in a workout}
\usage{
detect_sprints(
  motion_data,
  metric = c("speed", "power", "acceleration"),
  threshold = NULL,
  min_duration = 2,
  min_recovery = 2,
  smooth = TRUE
)
}
\arguments{
\item{motion_data}{A data frame with a numeric \code{time} (s) column and a
\code{velocity} (m/s) column (plus \code{metabolic_power}/\code{power} for \code{metric = "power"}).}

\item{metric}{Detection metric: \code{"speed"} (default), \code{"power"} or \code{"acceleration"}.}

\item{threshold}{Detection threshold in the metric's units. If \code{NULL} (default),
uses 5 m/s (speed), 20 W/kg (power) or 1.5 m/s^2 (acceleration).}

\item{min_duration}{Minimum effort duration to keep (s).}

\item{min_recovery}{Efforts separated by less than this (s) are merged.}

\item{smooth}{If \code{TRUE}, smooth the metric (~1 s rolling mean) before thresholding.}
}
\value{
A \link[tibble:tibble]{tibble} with one row per detected effort: \code{sprint_id},
\code{start_time}, \code{end_time}, \code{start_index}, \code{end_index}, \code{duration} (s),
\code{peak_speed} (m/s), \code{mean_speed} (m/s), \code{distance} (m) and
\code{peak_acceleration} (m/s^2).
}
\description{
Automatically segments a continuous workout into individual sprint efforts:
contiguous spans where the chosen metric (speed, power or acceleration) exceeds
a threshold, with brief dips merged and short blips discarded. Per-effort
summary metrics are returned.
}
\examples{
gpexe <- subset(ten_200_sprints_paired, source == "gpexe")
detect_sprints(gpexe)
}
