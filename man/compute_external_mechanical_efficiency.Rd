% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/external_power.R
\name{compute_external_mechanical_efficiency}
\alias{compute_external_mechanical_efficiency}
\title{Compute external mechanical efficiency over a run}
\usage{
compute_external_mechanical_efficiency(
  running_data_frame,
  cost_running_flat = 3.6,
  slope_equation = "extended",
  smoothing = "none",
  window = 3,
  order = 2,
  cfreq = 0.1
)
}
\arguments{
\item{running_data_frame}{A data frame with \code{time} (s) and \code{velocity} (m/s) columns.}

\item{cost_running_flat}{Flat-terrain cost of running (J/kg/m); see \code{\link[=cost_running]{cost_running()}}.}

\item{slope_equation}{Slope equation passed to \code{\link[=cost_running]{cost_running()}} (\code{"original"} or \code{"extended"}).}

\item{smoothing}{Velocity smoothing method passed to \code{\link[=smooth_velocity]{smooth_velocity()}}.}

\item{window}{Smoothing window length (samples).}

\item{order}{Smoothing / Butterworth order.}

\item{cfreq}{Butterworth normalised cut-off.}
}
\value{
The input data frame with \code{acceleration}, \code{cost_running},
\code{metabolic_power}, \code{external_power} and \code{external_mechanical_efficiency}
columns, filtered to admissible efficiencies.
}
\description{
Computes external power and metabolic power for a run and their ratio, the
external mechanical efficiency, keeping only physically admissible rows
(efficiency in the half-open interval (0, 1]).
}
\examples{
run <- tibble::tibble(time = 0:9,
                      velocity = c(0, 2, 4, 6, 7, 7.5, 7.6, 7.6, 7.5, 7.4))
compute_external_mechanical_efficiency(run)
}
