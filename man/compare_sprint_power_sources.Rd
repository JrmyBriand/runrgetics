% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/device_comparison.R
\name{compare_sprint_power_sources}
\alias{compare_sprint_power_sources}
\title{Compare watch speed/power derivations for one sprint}
\usage{
compare_sprint_power_sources(
  paired_data,
  sprint_id = 1,
  sprints = NULL,
  body_mass = 67,
  filter_method = "butterworth",
  cutoff = 0.175,
  target_hz = 5,
  cost_running_flat = 3.6,
  slope_equation = "extended",
  reference = "gpexe",
  device = "polar_stryd",
  margin = 3
)
}
\arguments{
\item{paired_data}{A paired-device data frame with a \code{source} column (e.g.
\link{ten_200_sprints_paired}); the device rows must carry a \code{power_w} (Stryd, W)
column.}

\item{sprint_id}{Which detected sprint to compare (its \code{sprint_id}).}

\item{sprints}{Optional sprint table from \code{\link[=detect_sprints]{detect_sprints()}} (detected on the
reference); if \code{NULL} (default), sprints are detected.}

\item{body_mass}{Athlete body mass (kg) used to convert the Stryd power to W/kg.}

\item{filter_method, cutoff, target_hz}{Passed to \code{\link[=filter_watch_motion]{filter_watch_motion()}}.}

\item{cost_running_flat}{Flat-terrain cost of running (J/kg/m).}

\item{slope_equation}{Slope equation passed to \code{\link[=cost_running]{cost_running()}}.}

\item{reference, device}{Values of the \code{source} column identifying the reference
(gpexe) and watch rows.}

\item{margin}{Seconds of data to include on each side of the detected sprint.}
}
\value{
A \link[tibble:tibble]{tibble} with columns \code{time} (s, from the sprint
start), \code{panel}, \code{source} (\code{"watch"}, \code{"gps"}, \code{"stryd"}) and \code{value}.
}
\description{
For a single detected sprint, derives the watch motion from two sources -
the watch's reported speed and speed derived from raw GPS position - each
filtered with \code{\link[=filter_watch_motion]{filter_watch_motion()}}, and assembles a tidy comparison of four
channels: speed, acceleration, metabolic power (\code{cost_running()}) and external
power (\code{external_power()}). The external-power panel also includes the Stryd
unit's measured power (\code{power_w}) expressed per kilogram using \code{body_mass}.
}
\examples{
compare_sprint_power_sources(ten_200_sprints_paired, sprint_id = 1, body_mass = 67)
}
