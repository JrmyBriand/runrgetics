% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_running_power.R
\name{compute_metabolic_running_power}
\alias{compute_metabolic_running_power}
\title{Compute metabolic running power over a run}
\usage{
compute_metabolic_running_power(
  running_data_frame,
  cost_running_flat = 3.6,
  slope_equation = "extended",
  smoothing = "none",
  window = 3,
  order = 2,
  cfreq = 0.1
)
}
\arguments{
\item{running_data_frame}{A data frame with \code{time} (s) and \code{velocity} (m/s) columns.}

\item{cost_running_flat}{Flat-terrain cost of running (J/kg/m); see \code{\link[=cost_running]{cost_running()}}.}

\item{slope_equation}{Slope equation passed to \code{\link[=cost_running]{cost_running()}} (\code{"original"} or \code{"extended"}).}

\item{smoothing}{Velocity smoothing method passed to \code{\link[=smooth_velocity]{smooth_velocity()}}.}

\item{window}{Smoothing window length (samples).}

\item{order}{Smoothing / Butterworth order.}

\item{cfreq}{Butterworth normalised cut-off.}
}
\value{
The input data frame with \code{acceleration} (m/s^2), \code{cost_running}
(J/kg/m) and \code{metabolic_power} (W/kg) columns added.
}
\description{
Smooths velocity (optionally), computes acceleration and the instantaneous
metabolic running power as the cost of running times velocity, using the sprint
cost-of-running model (\code{\link[=cost_running]{cost_running()}}).
}
\examples{
run <- tibble::tibble(time = 0:9,
                      velocity = c(0, 2, 4, 6, 7, 7.5, 7.6, 7.6, 7.5, 7.4))
compute_metabolic_running_power(run)
}
