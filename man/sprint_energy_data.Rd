% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sprint_energy.R
\name{sprint_energy_data}
\alias{sprint_energy_data}
\title{Cumulative energy by pathway for one sprint}
\usage{
sprint_energy_data(
  motion_data,
  sprint_id = 1,
  sprints = NULL,
  maximal_aerobic_power = 27,
  ...
)
}
\arguments{
\item{motion_data}{A workout data frame with \code{time} (s) and \code{velocity} (m/s).}

\item{sprint_id}{Which detected sprint to plot (its \code{sprint_id}).}

\item{sprints}{Optional sprint table from \code{\link[=detect_sprints]{detect_sprints()}}; if \code{NULL} (default),
sprints are detected with \code{\link[=detect_sprints]{detect_sprints()}}.}

\item{maximal_aerobic_power}{Maximal aerobic power, MAP (W/kg). Use 27 for the
\code{sprint_mix} / \code{ten_200_sprints} sessions.}

\item{...}{Additional arguments forwarded to the bioenergetic fit (e.g.
\code{fit_mu}, \code{fit_sigma}, \code{fit_k2}, \code{trim}, \code{decel_threshold}).}
}
\value{
A \link[tibble:tibble]{tibble} (long) with \code{sprint_id}, \code{time} (s),
\code{distance} (m), \code{pathway} (\code{total}/\code{alactic}/\code{lactic}/\code{aerobic}) and
\code{cumulative_energy} (J/kg).
}
\description{
Decomposes a sprint's metabolic power (alactic / lactic / aerobic) and returns
the cumulative energy (running integral of power, J/kg) of each pathway, indexed
by both time and distance from the sprint start. The cumulative \code{total} is the
sum of the three pathways.
}
\examples{
gpexe <- subset(ten_200_sprints_paired, source == "gpexe")
head(sprint_energy_data(gpexe, sprint_id = 1, maximal_aerobic_power = 27))
}
