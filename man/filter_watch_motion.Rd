% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/watch_filter.R
\name{filter_watch_motion}
\alias{filter_watch_motion}
\title{Filter watch motion data to emulate the gpexe-filtered signal}
\usage{
filter_watch_motion(
  data,
  method = c("butterworth", "savitzky_golay", "moving_average"),
  cutoff = 0.175,
  order = 2,
  window = 11,
  target_hz = 5,
  speed_source = c("reported", "gps")
)
}
\arguments{
\item{data}{a data frame of watch data with a numeric \code{time} column (s) and either
a \code{velocity} column (m/s, for \code{speed_source = "reported"}) or \code{latitude} /
\code{longitude} columns (for \code{speed_source = "gps"}).}

\item{method}{low-pass filter family: \code{"butterworth"} (default, zero-phase),
\code{"savitzky_golay"} or \code{"moving_average"}.}

\item{cutoff}{Butterworth cut-off frequency (Hz); used when \code{method = "butterworth"}.
The default (0.175 Hz) was tuned against the paired datasets (see \code{\link[=tune_watch_filter]{tune_watch_filter()}}).}

\item{order}{filter order (Butterworth) or polynomial order (Savitzky-Golay).}

\item{window}{window length in samples (Savitzky-Golay / moving average).}

\item{target_hz}{uniform resampling rate (Hz) used before filtering.}

\item{speed_source}{\code{"reported"} to filter the watch's speed column, or \code{"gps"}
to filter speed derived from GPS position via \code{\link[=gps_speed]{gps_speed()}}.}
}
\value{
a \link[tibble:tibble]{tibble} on the uniform grid with columns \code{time},
\code{velocity} (m/s), \code{acceleration} (m/s^2), \code{distance} (m) and, when position is
supplied, smoothed \code{latitude} / \code{longitude}.
}
\description{
Low-pass filters noisy Polar/Stryd watch data so that the resulting speed,
acceleration and distance approximate the gpexe-filtered reference. The signal
is resampled onto a uniform grid (\code{target_hz}), filtered with a zero-phase filter
(so no time lag is introduced), and acceleration / cumulative distance are derived
from the filtered speed. If \code{latitude} / \code{longitude} are present they are smoothed
with the same filter. Default parameters were tuned against the paired datasets
(see \code{\link[=tune_watch_filter]{tune_watch_filter()}}).
}
\examples{
w <- subset(sprint_mix_paired, source == "polar_stryd")
filtered <- filter_watch_motion(w)
head(filtered)
}
