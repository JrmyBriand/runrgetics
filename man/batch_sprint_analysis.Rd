% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sprint_session.R
\name{batch_sprint_analysis}
\alias{batch_sprint_analysis}
\title{Batch-analyse a set of gpexe session files}
\usage{
batch_sprint_analysis(
  files,
  output_dir,
  maximal_aerobic_power = 20,
  labels = NULL,
  width = 9,
  height = 5,
  dpi = 110
)
}
\arguments{
\item{files}{Character vector of gpexe \code{.csv}/\code{.csv.gz} paths.}

\item{output_dir}{Directory to write results to (created if needed).}

\item{maximal_aerobic_power}{MAP (W/kg); a scalar applied to all files, or a
vector recycled to \code{length(files)} for per-session values.}

\item{labels}{Output sub-folder name per file (default: file name without extension).}

\item{width, height, dpi}{Figure size / resolution passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
(invisibly) a data frame run log with one row per file (\code{file}, \code{label},
\code{n_sprints}, \code{map}, \code{status}, \code{message}).
}
\description{
Reads each gpexe CSV, runs \code{\link[=analyze_sprint_session]{analyze_sprint_session()}}, and writes per-session
tables (CSV) and figures (PNG) under \verb{output_dir/<label>/}, plus a \code{run_log.csv}
summarising the batch. Each file is processed independently; a file that errors
(or has no detectable sprint) is recorded in the log and does not stop the batch.
}
\examples{
\donttest{
gpexe <- subset(ten_200_sprints_paired, source == "gpexe")
f <- tempfile(fileext = ".csv")
utils::write.table(data.frame("time (s)" = gpexe$time,
                              "speed (km/h)" = gpexe$velocity * 3.6,
                              check.names = FALSE),
                   f, sep = ";", dec = ".", row.names = FALSE)
batch_sprint_analysis(f, output_dir = tempfile("sls"), maximal_aerobic_power = 20)
}
}
