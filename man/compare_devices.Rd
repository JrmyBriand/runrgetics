% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/device_comparison.R
\name{compare_devices}
\alias{compare_devices}
\title{Compare watch and gpexe motion and power signals}
\usage{
compare_devices(
  paired_data,
  speed_source = "reported",
  filter_method = "butterworth",
  cutoff = 0.175,
  target_hz = 5,
  cost_running_flat = 3.6,
  slope_equation = "extended",
  reference = "gpexe",
  device = "polar_stryd"
)
}
\arguments{
\item{paired_data}{A paired-device data frame with a \code{source} column (e.g.
\link{sprint_mix_paired}) containing both the reference and watch rows.}

\item{speed_source}{Watch speed input passed to \code{\link[=filter_watch_motion]{filter_watch_motion()}}.}

\item{filter_method}{Watch filter family passed to \code{\link[=filter_watch_motion]{filter_watch_motion()}}.}

\item{cutoff}{Watch filter cut-off (Hz).}

\item{target_hz}{Common comparison grid rate (Hz).}

\item{cost_running_flat}{Flat-terrain cost of running (J/kg/m) for metabolic power.}

\item{slope_equation}{Slope equation passed to \code{\link[=cost_running]{cost_running()}}.}

\item{reference, device}{Values of the \code{source} column identifying the reference
(gpexe) and watch rows.}
}
\value{
A \link[tibble:tibble]{tibble} with one row per channel (\code{speed},
\code{acceleration}, \code{distance}, \code{external_power}, \code{metabolic_power}) and columns
\code{rmse}, \code{bias}, \code{cor}, \code{watch_mean}, \code{ref_mean}, \code{ratio} and \code{n}.
}
\description{
Filters the Polar/Stryd watch signal with the tuned watch filter
(\code{\link[=filter_watch_motion]{filter_watch_motion()}}), derives matched motion and power channels for both
the watch and the gpexe reference on a common grid, and reports their agreement
(speed, acceleration, distance, external power, metabolic power). gpexe is the
reference; positive \code{bias}/\code{ratio > 1} means the watch reads higher.
}
\examples{
compare_devices(sprint_mix_paired)
}
