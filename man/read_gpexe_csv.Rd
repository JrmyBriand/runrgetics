% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sprint_session.R
\name{read_gpexe_csv}
\alias{read_gpexe_csv}
\title{Read a gpexe CSV export into a motion data frame}
\usage{
read_gpexe_csv(file)
}
\arguments{
\item{file}{Path to a gpexe \code{.csv} (or \code{.csv.gz}) export.}
}
\value{
A \link[tibble:tibble]{tibble} with \code{time} (s) and \code{velocity} (m/s), plus any
of \code{raw_speed}, \code{acc}, \code{power_w_kg}, \code{external_power_positive}, \code{gpexe_distance},
\code{latitude}, \code{longitude} that were present.
}
\description{
Reads a gpexe semicolon-separated CSV (decimal point) and returns a tidy motion
data frame with \code{time} (s) and \code{velocity} (m/s, from the filtered \code{speed (km/h)}
column). Available extra channels (raw speed, acceleration, power, position) are
carried through. Column order is irrelevant (columns are matched by name).
Gzipped files (\code{.csv.gz}) are read transparently.
}
\examples{
\donttest{
f <- system.file("extdata", "sprint_mix_gpexe.csv.gz", package = "runrgetics")
if (nzchar(f)) head(read_gpexe_csv(f))
}
}
