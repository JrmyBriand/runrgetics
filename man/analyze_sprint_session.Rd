% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sprint_session.R
\name{analyze_sprint_session}
\alias{analyze_sprint_session}
\title{Analyse a sprint-training session}
\usage{
analyze_sprint_session(motion_data, maximal_aerobic_power = 20, ...)
}
\arguments{
\item{motion_data}{A data frame with \code{time} (s) and \code{velocity} (m/s), e.g. from
\code{\link[=read_gpexe_csv]{read_gpexe_csv()}}.}

\item{maximal_aerobic_power}{Maximal aerobic power, MAP (W/kg). Default 20.}

\item{...}{Passed to \code{\link[=detect_sprints]{detect_sprints()}}.}
}
\value{
A list with \code{sprints} (\code{\link[=detect_sprints]{detect_sprints()}} table), \code{workout}
(\code{\link[=compare_workout_sprints]{compare_workout_sprints()}} table) and \code{bioenergetics}
(\code{\link[=analyze_training_bioenergetics]{analyze_training_bioenergetics()}} list), plus \code{maximal_aerobic_power}. The
workout/bioenergetics entries are \code{NULL} if no sprint is detected.
}
\description{
Runs the full sprint-training pipeline on one session's motion data: detects the
sprints, compares them across the workout, and decomposes their metabolic power
into alactic / lactic / aerobic contributions.
}
\examples{
\donttest{
gpexe <- subset(ten_200_sprints_paired, source == "gpexe")
res <- analyze_sprint_session(gpexe, maximal_aerobic_power = 20)
res$bioenergetics$summary
}
}
