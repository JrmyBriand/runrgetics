% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/device_comparison.R
\name{plot_sprint_power_sources}
\alias{plot_sprint_power_sources}
\title{Plot the watch speed/power source comparison for one sprint}
\usage{
plot_sprint_power_sources(
  paired_data,
  sprint_id = 1,
  sprints = NULL,
  body_mass = 67,
  ...
)
}
\arguments{
\item{paired_data}{A paired-device data frame with a \code{source} column (e.g.
\link{ten_200_sprints_paired}); the device rows must carry a \code{power_w} (Stryd, W)
column.}

\item{sprint_id}{Which detected sprint to compare (its \code{sprint_id}).}

\item{sprints}{Optional sprint table from \code{\link[=detect_sprints]{detect_sprints()}} (detected on the
reference); if \code{NULL} (default), sprints are detected.}

\item{body_mass}{Athlete body mass (kg) used to convert the Stryd power to W/kg.}

\item{...}{Additional arguments passed to \code{\link[=compare_sprint_power_sources]{compare_sprint_power_sources()}}.}
}
\value{
A ggplot object (2x2 facets).
}
\description{
Four-panel comparison for a single sprint of the watch's reported-speed and
GPS-derived derivations across speed, acceleration, metabolic power and external
power, with the Stryd-measured external power overlaid on the external-power panel.
}
\examples{
plot_sprint_power_sources(ten_200_sprints_paired, sprint_id = 1, body_mass = 67)
}
