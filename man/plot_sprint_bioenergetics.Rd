% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/training_bioenergetics.R
\name{plot_sprint_bioenergetics}
\alias{plot_sprint_bioenergetics}
\title{Plot a sprint's bioenergetic decomposition}
\usage{
plot_sprint_bioenergetics(
  motion_data,
  sprint_id = 1,
  sprints = NULL,
  maximal_aerobic_power = 27,
  trim = TRUE,
  decel_threshold = 1,
  launch_accel = 0.5,
  cost_running_flat = 3.6,
  slope_equation = "extended",
  mu = -0.4,
  sigma = 1,
  k1 = 2.75,
  k2 = 35,
  fit_mu = TRUE,
  fit_sigma = TRUE
)
}
\arguments{
\item{motion_data}{A workout data frame with \code{time} (s) and \code{velocity} (m/s).}

\item{sprint_id}{Which detected sprint to plot (its \code{sprint_id}).}

\item{sprints}{Optional sprint table from \code{\link[=detect_sprints]{detect_sprints()}}; if \code{NULL} (default),
sprints are detected with \code{\link[=detect_sprints]{detect_sprints()}}.}

\item{maximal_aerobic_power}{Maximal aerobic power, MAP (W/kg). Use 27 for the
\code{sprint_mix} / \code{ten_200_sprints} sessions.}

\item{trim}{If \code{TRUE} (default), trim the pre-launch lead-in and the
end-of-effort deceleration before fitting.}

\item{decel_threshold}{Deceleration threshold (m/s^2) for the end-of-effort trim.}

\item{launch_accel}{Acceleration threshold (m/s^2) for the launch trim.}

\item{cost_running_flat}{Flat-terrain cost of running (J/kg/m).}

\item{slope_equation}{Slope equation passed to \code{\link[=cost_running]{cost_running()}}.}

\item{mu, sigma, k1, k2}{Sprint bioenergetic model shape parameters
(see \code{\link[=sprint_bioenergetic_model]{sprint_bioenergetic_model()}}). \code{mu} is the starting value when \code{fit_mu}
is \code{TRUE}, otherwise the fixed value.}

\item{fit_mu}{If \code{TRUE} (default), also estimate the alactic peak location \code{mu}
so it adapts to the observed metabolic-power peak (recommended for gpexe /
training sprints, whose power peaks later than in maximal sprints); if \code{FALSE},
\code{mu} is held at the published fixed value.}

\item{fit_sigma}{If \code{TRUE} (default), also estimate the alactic width \code{sigma}
(letting the bump narrow / decay faster); fitting \code{sigma} together with \code{mu}
keeps the alactic separated from the lactic term and the contributions
physiological. If \code{FALSE}, \code{sigma} is held fixed.}
}
\value{
A ggplot object.
}
\description{
Decomposes one sprint of a workout into its alactic, lactic and aerobic
metabolic-power contributions and plots them together with the observed and
modeled total metabolic power, in the shared package style. The sprint is
extracted over its full effort and launch-aligned, so the alactic peak matches
the real acceleration burst.
}
\examples{
gpexe <- subset(ten_200_sprints_paired, source == "gpexe")
plot_sprint_bioenergetics(gpexe, sprint_id = 1, maximal_aerobic_power = 27)
}
