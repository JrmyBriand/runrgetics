% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dynamic_bioenergetics.R
\name{simulate_dynamic_bioenergetics}
\alias{simulate_dynamic_bioenergetics}
\title{Simulate a sprint's bioenergetics with the dynamic metabolic model}
\usage{
simulate_dynamic_bioenergetics(
  power_demand,
  time,
  vo2max,
  vlamax,
  sc = 25,
  sa = 7,
  m_active = 0.25,
  pcr_0 = 23,
  initial_bla = 1.1
)
}
\arguments{
\item{power_demand}{Numeric vector of metabolic-power demand (W/kg), the series
the model reproduces (the same di Prampero demand the sprint bioenergetic
model fits).}

\item{time}{Numeric vector of time points (s), strictly increasing, the same
length as \code{power_demand}.}

\item{vo2max}{Maximal oxygen uptake (mL/kg/min, systemic). For the dynamic
model use the measured value over-estimated by ~5\%.}

\item{vlamax}{Maximal glycolytic rate (mmol/kg/s, systemic).}

\item{sc}{Creatine pool size (mmol/kg). Default 25 (MuscleEdot default).}

\item{sa}{Adenylate pool size (mmol/kg). Default 7 (MuscleEdot default).}

\item{m_active}{Active muscle mass fraction. Default 0.25 (MuscleEdot default).}

\item{pcr_0}{Initial phosphocreatine concentration (mmol/kg). Default 23.}

\item{initial_bla}{Initial blood lactate (mmol/L). Default 1.1.}
}
\value{
A \link[tibble:tibble]{tibble} with columns \code{time} (s), \code{measured}
(input demand, W/kg), and \code{alactic}, \code{lactic}, \code{aerobic}, \code{total}
(modelled power, W/kg). Attributes \code{energy_alactic}, \code{energy_lactic},
\code{energy_aerobic}, \code{energy_total} (J/kg) and \code{vlamax} are attached.
}
\description{
Integrates a measured metabolic-power demand (e.g. the di Prampero sprint cost
of running x velocity) through the dynamic Mader-style 5-state ODE model ported
from MuscleEdot, and partitions the response into instantaneous aerobic
(oxidative phosphorylation), glycolytic (lactic) and alactic (phosphocreatine)
power. The output columns mirror the sprint bioenergetic model
(\code{\link[=analyze_sprint_bioenergetics]{analyze_sprint_bioenergetics()}}) so the two decompositions are directly
comparable. Time-integrated energies (J/kg) and the VLamax used are attached as
attributes.
}
\examples{
# Drive the dynamic model with a metabolic-power demand profile (W/kg)
t <- seq(0, 20, by = 0.2)
demand <- 25 * (1 - exp(-t / 3))
sim <- simulate_dynamic_bioenergetics(demand, t, vo2max = 78.75, vlamax = 0.5)
head(sim)
attr(sim, "energy_total")
}
