% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sprint_energy.R
\name{plot_workout_energy}
\alias{plot_workout_energy}
\title{Plot cumulative energy by pathway across a workout's sprints}
\usage{
plot_workout_energy(
  motion_data,
  sprints = NULL,
  maximal_aerobic_power = 27,
  x = c("distance", "time"),
  ...
)
}
\arguments{
\item{motion_data}{A workout data frame with \code{time} (s) and \code{velocity} (m/s).}

\item{sprints}{Optional sprint table from \code{\link[=detect_sprints]{detect_sprints()}}; if \code{NULL} (default),
sprints are detected.}

\item{maximal_aerobic_power}{Maximal aerobic power, MAP (W/kg). Use 27 for the
\code{sprint_mix} / \code{ten_200_sprints} sessions.}

\item{x}{Horizontal axis: \code{"distance"} (m, default) or \code{"time"} (s).}

\item{...}{Additional arguments passed to \code{\link[=sprint_energy_data]{sprint_energy_data()}}.}
}
\value{
A ggplot object (faceted by pathway).
}
\description{
Superposes the workout's sprints to compare how cumulative energy builds with
distance (or time). Faceted by pathway (\code{total}, \code{alactic}, \code{lactic}, \code{aerobic}),
one coloured line per sprint. The aerobic curves typically superimpose closely
(similar duration and maximal aerobic power), while the anaerobic pathways spread
the inter-sprint differences.
}
\examples{
gpexe <- subset(ten_200_sprints_paired, source == "gpexe")
plot_workout_energy(gpexe, maximal_aerobic_power = 27)
}
